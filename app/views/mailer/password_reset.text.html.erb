<% inst = InstallationHelper.CURRENT_INSTALLATION %>
Someone has requested a password reset for your account at http://<%= inst.talksSiteHost %>

If you did not request a password change, you can safely ignore this email.

Please click on the following link, where a new password will be generated for you.

<%=link_to("Reset my password", reset_password_url(:id => @user.id, :password_reset_key => @user.password_reset_key)) %>

Please let us know if you have any problems:

<%= inst.webmasterEmail %>

(Note that this is a different e-mail address from the one that this message is sent from)
